export const Label = ({children}) => {
    return (
        <div
            className="block m-2 text-sm font-medium text-gray-900 dark:text-white"
        >
            {children}
        </div>

    )
}